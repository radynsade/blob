use human.Human;
use human.newHuman;
use human.getFirstName;
use human.getLastName;
use human.FirstName;
use human.LastName;
use std.out.println;

public const main := function () do
	var human: Human := newHuman('Bobby', 'Benson');
	var firstName: FirstName := getFirstName(human);
	println(firstName); // 'Bobby'

end
